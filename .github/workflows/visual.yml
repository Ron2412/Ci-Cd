name: Visual Graph Pipeline

on:
  workflow_dispatch:
jobs:
      job-1:
        runs-on: ubuntu-latest
        steps:
          - name: A1
            run: echo "A1"
      job-2:
        runs-on: ubuntu-latest
        needs: 
        - job-1
        steps:
          - name: B1
            run: echo "B1"
      job-3:
        runs-on: ubuntu-latest
        needs:
          - job-1
          - job-2
        steps:
          - name: C1
            run: echo "C1"
    
